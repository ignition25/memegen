<div class="row">
  <div class="col-md-1">
    <ul class="nav nav-pills nav-stacked">
      <li class="active"><a href="/">Newest</a></li>
    </ul>
  </div>
  <!-- End navigation -->
  <div class="col-md-11">
      <div class="memes-container">
        <% @memes.each do |meme| %>
          <a href="<%= meme_path(meme) %>"><img src="<%= getMemePath(meme) %>" class="meme" /></a>
        <% end %>
      </div>
    <br>
  </div>
</div>
